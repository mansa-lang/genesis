[project]
name = "genesis"
version = "0.1.0"
description = "The Python bootstrap compiler for the Mansa programming language"
readme = "README.md"
requires-python = ">=3.15"
dependencies = [
    "lark>=1.3.1",
    "pytest>=9.1.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.0",
    "ruff>=0.15.17",
]
